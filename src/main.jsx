import React from 'react' // API de react
import ReactDOM from 'react-dom/client' // Version WEB de React
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  // <React.StrictMode> // Testing
    <App />
  // </React.StrictMode>,
)
